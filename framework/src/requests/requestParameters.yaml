$schema: https://json-schema.org/draft/2020-12/schema
description: >-
  The schema below is a placeholder for a dictionary 'key':'object'. The individual
  definitions depend on the model (e.g. beacon-v2-default-model) and entity (e.g.
  `genomicVariant`).
type: object
properties:
  $schema:
    type: string
    description: >-
      Added here to allow proper validation of the documents w/o providing specific
      values.
additionalProperties:
  type: object

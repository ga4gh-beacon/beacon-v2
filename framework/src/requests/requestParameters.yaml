$schema: https://json-schema.org/draft/2020-12/schema
description: The schema below is suited for a dictionary 'key':'object', which is
  the case of the parameter definitions.
type: object
properties:
  $schema:
    type: string
    description: Added here to allow proper validation of the documents.
additionalProperties:
  type: object

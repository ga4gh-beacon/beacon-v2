{
    "$defs": {
        "AggregationTermInRequest": {
            "description": "An object representing an individual term to be aggregated, e.g. a term corresponding to `sexAtBirth` or `diagnosis`. The `id` property is required and identifies the term to be aggregated, while the `filters` and `splits` properties are optional modifiers of the aggregation term. Note: The request object does not specifiy the property or scope of the aggregation.",
            "properties": {
                "id": {
                    "description": "The `id` of the aggregation term as provided by the beacon. It is used to select the aggregation term, _i.e._ usually corresponding to a property in the data model. ",
                    "examples": [
                        {
                            "id": "selectedDiagnoses"
                        },
                        {
                            "id": "biosample.histologicalDiagnosis.id"
                        },
                        {
                            "id": "sexAtBirth"
                        },
                        {
                            "id": "ageAtDiagnosis"
                        }
                    ],
                    "type": "string"
                },
                "selectors": {
                    "description": "List of selectors to select specific values of the given property for aggregation, as a modifier of the aggregation term. For example, for an aggregation term of `diagnosis` selectors could be used to specify a subset of diagnoses to be aggregated.",
                    "items": {
                        "oneOf": [
                            {
                                "$ref": "#/$defs/ValueForAggregation"
                            },
                            {
                                "$ref": "#/$defs/RangeForAggregation"
                            },
                            {
                                "$ref": "#/$defs/SplitForAggregation"
                            }
                        ]
                    },
                    "minItems": 1,
                    "type": "array"
                }
            },
            "required": [
                "id"
            ],
            "type": "object"
        },
        "AggregationsInRequest": {
            "description": "A principal aggregation directive, indicating which of the single property aggregations indicated in a beacon's `/aggregation_terms` should be evaluated, alone or in combination.",
            "examples": [
                {
                    "aggregationTerms": [
                        {
                            "id": "Sex"
                        }
                    ],
                    "requestId": "aggr1"
                },
                {
                    "aggregationTerms": [
                        {
                            "id": "Sex",
                            "selectors": [
                                {
                                    "value": "NCIT:C16576"
                                },
                                {
                                    "value": "NCIT:C20197"
                                }
                            ]
                        }
                    ],
                    "requestId": "aggr2"
                },
                {
                    "aggregationTerms": [
                        {
                            "id": "SexAtBirth"
                        },
                        {
                            "id": "HistologicalDiagnoses",
                            "selectors": [
                                {
                                    "value": "NCIT:C2919"
                                },
                                {
                                    "value": "NCIT:C4017"
                                },
                                {
                                    "value": "NCIT:C3512"
                                }
                            ]
                        }
                    ],
                    "requestId": "aggr3"
                },
                {
                    "aggregationTerms": [
                        {
                            "id": "AgeOfOnset"
                        }
                    ],
                    "requestId": "aggr4"
                }
            ],
            "properties": {
                "aggregationTerms": {
                    "description": "An array of one or more `AggregationTermInRequest` objects representing the properties to be aggregated, e.g. terms corresponding to `sexAtBirth` and `diagnosis`.",
                    "items": {
                        "$ref": "#/$defs/AggregationTermInRequest"
                    },
                    "type": "array"
                },
                "requestId": {
                    "description": "A process id for the aggregation request, which can be used for tracking and logging purposes. This is not used by the beacon for interpreting the request but can be included in the response metadata.",
                    "type": "string"
                }
            },
            "required": [
                "requestId",
                "aggregationTerms"
            ],
            "type": "object"
        },
        "RangeForAggregation": {
            "examples": [
                {
                    "label": "birth to 18 months",
                    "rangeBounds": [
                        "P0M",
                        "P18M"
                    ],
                    "sortOrder": 1
                }
            ],
            "properties": {
                "label": {
                    "type": "string"
                },
                "rangeBounds": {
                    "description": "The limits of the range, representing inclusive lower bounds to exclusive upper bounds. When only one value is given, it represents the lower bound and the upper bound is considered unbounded. The format of the values should be consistent with the type of the property being split, e.g. ISO8601 durations for time intervals or numeric values for continuous numeric variables. ",
                    "items": {
                        "type": [
                            "string",
                            "number"
                        ]
                    },
                    "minItems": 1,
                    "type": "array"
                },
                "sortOrder": {
                    "description": "Integer to specify the intended order of splits. This is required to ensure the correct order of bins in the results, or for the client to be able to reconstruct the expected order in case any of the intermediate steps reorders them.",
                    "type": "integer"
                }
            },
            "required": [
                "rangeBounds",
                "sortOrder"
            ],
            "type": "object"
        },
        "SplitForAggregation": {
            "description": "A split item for partitioning the values during aggregation.",
            "examples": [
                {
                    "label": "younger than 65 years",
                    "sortOrder": 1,
                    "splitValue": "P65Y"
                },
                {
                    "label": "65 years and older",
                    "sortOrder": 2,
                    "splitValue": "P150Y"
                },
                {
                    "label": "25kg or higher",
                    "sortOrder": 3,
                    "splitValue": 25
                }
            ],
            "properties": {
                "label": {
                    "description": "A human readable label for the bin resulting from the split.",
                    "type": "string"
                },
                "sortOrder": {
                    "description": "Integer to specify the intended order of splits. This is required to ensure the correct order of bins in the results, or for the client to be able to reconstruct the expected order in case any of the intermediate steps reorders them.",
                    "type": "integer"
                },
                "splitValue": {
                    "description": "A value at which the split should occur in the properties value space. The format of the value should be consistent with the type of the property being split, e.g. ISO8601 durations for time intervals or numeric values for continuous numeric variables. Split values are exclusive upper bounds for the resulting bins, meaning that values equal to the split value will be included in the next bin. Note: To cover the complete value space one might use a (last) split value that extends beyond the maximum value in the data, e.g. `P150Y` for age at diagnosis.",
                    "type": [
                        "string",
                        "number"
                    ]
                }
            },
            "required": [
                "splitValue",
                "sortOrder"
            ],
            "type": "object"
        },
        "ValueForAggregation": {
            "description": "An object representing a specific value of the property on which the aggregation is performed, used as a modifier of the aggregation term to select specific values for aggregation. For example, for an aggregation term of `diagnosis` this could be used to specify a specific diagnosis to be aggregated on.",
            "properties": {
                "examples": [
                    {
                        "label": "Kidney Carcinoma",
                        "value": "NCIT:C9384"
                    },
                    {
                        "value": "female"
                    }
                ],
                "label": {
                    "description": "An optional label for the value, e.g. to allow the service to return in an informative response.",
                    "type": "string"
                },
                "required": [
                    "value"
                ],
                "value": {
                    "description": "The specific value of the category or value to be aggregated, e.g. an ontology term CURIE an alphanumeric value.",
                    "type": "string"
                }
            },
            "type": "object"
        }
    },
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "description": "Terms used to specify items to return in an aggregation data response. Terms\nare provided through the beacon's `/aggregation_terms` endpoint and the `id`\nvalues of those terms constitute the possible `id` values here. See also the\ndocumentation for `AggregationTermInResponse.id` in `beaconAggregationTermsResults`.\n\nThe request for aggregations is a list of aggregation objects. The individual \naggregation request objects contain lists of 1 or more (but mostly 1 or 2)\naggregation terms which correspond to individual properties and - if more than\none - are evaluated together, e.g. \"diagnoses (1) for sex at birth (2)\".",
    "items": {
        "$ref": "#/$defs/AggregationsInRequest"
    },
    "title": "Aggregation Terms",
    "type": "array"
}
{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "description": "Terms used to specify fields to return in a summary data response. \nIn practice we use the the \"id\" field from existing filtering terms but avoid\nthe additional modifiers, but addditionally allows the use of custom summary\nstructures beyond aggregaion corresponding to query filters; e.g.\n* a `survivalGroups` term could aggregate data by survival groups\n* a `cnvfrequencies` term could envoke a list of objects for gain, loss, ...\n  events per genomic interval in the matched data",
    "items": {
        "examples": [
            "UBERON:0000992",
            "NCIT:C4908",
            "sex",
            "ageAtDiagnosis<=P18Y",
            "survivalGroups",
            "cnvfrequencies"
        ],
        "type": "string"
    },
    "type": "array"
}
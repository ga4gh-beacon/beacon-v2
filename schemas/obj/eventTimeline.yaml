---
eventTimeline:
  description: Aggregated information of dates of visit/diagnostic/inclusion in study
    obtained from individual level info in database. Will coincide with collection
    event date for multi-time
  properties:
    end:
      description: latest date of visit
      format: datetime
      type: string
    start:
      description: earliest date of visit
      format: datetime
      type: string
  type: object

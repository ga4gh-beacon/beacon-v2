---
end:
  description: Precise or bracketing the end of the variant, for variants not specified
    by their sequence content (e.g. structural variants, particularly CNVs).
  examples:
  - []
  - - 1200
  - - 1200
    - 1350
  items:
    format: int64
    minimum: 1
    type: integer
  maxItems: 2
  minItems: 0
  type: array
